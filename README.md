# Промо-лендінг 11.11 для Meest China Shop

Промо-лендінг присвячений розпродажу на 11.11 (Singles' Day) для продукту Meest Shop. Це репозиторій з вихідним кодом лендінгу.

## 📋 Опис

Лендінг-сторінка для промо-акції 11.11 з інформацією про:
- Спеціальні пропозиції та знижки
- Маркетплейси, з яких можна замовляти
- Інструкції як замовити
- Переваги сервісу Meest China
- Таймер до початку акції

## 🚀 Технології

- **HTML5** - семантична розмітка
- **CSS3** - стилізація з використанням normalize.css
- **JavaScript (Vanilla)** - функціональність без залежностей
- **Webpack 5** - збірка проєкту
- **Stylelint** - перевірка якості CSS коду

## 📦 Вимоги

- **Node.js** версія 20 або вище
- **npm** або **yarn**

## 🔧 Встановлення

1. Клонуйте репозиторій:
```bash
git clone <repository-url>
cd landing_11-11
```

2. Встановіть залежності:
```bash
npm install
```

## 🛠️ Доступні скрипти

### Розробка

```bash
npm start
```
Запускає webpack dev server з автоматичним відкриттям браузера та hot reload.

### Збірка

```bash
npm run build
```
Створює production збірку в директорії `dist/`.

```bash
npm run build:github
```
Створює збірку для GitHub Pages з `PUBLIC_PATH=/landing_11-11`.

```bash
npm run build:production
```
Створює production збірку з Google Tag Manager ID (`GTM-MFD96JSQ`).

### Лінтування CSS

```bash
npm run lint:css
```
Перевіряє всі CSS файли на помилки.

```bash
npm run lint:css:fix
```
Автоматично виправляє помилки в CSS файлах.

```bash
npm run lint:css:style
```
Перевіряє тільки `css/style.css`.

```bash
npm run lint:css:style:fix
```
Автоматично виправляє помилки в `css/style.css`.

## 📁 Структура проєкту

```
landing_11-11/
├── css/
│   ├── normalize.css      # Скидання стилів браузера
│   └── style.css          # Основні стилі
├── js/
│   ├── app.js             # Основна логіка JavaScript
│   └── vendor/            # Сторонні бібліотеки (якщо є)
├── img/                   # Зображення та іконки
│   ├── icons/             # SVG іконки
│   ├── marketplaces/      # Логотипи маркетплейсів
│   ├── products/          # Зображення продуктів
│   └── section-*/         # Зображення для секцій
├── dist/                  # Зібрана версія (генерується)
├── index.html             # Головний HTML файл
├── webpack.common.js      # Загальна конфігурація webpack
├── webpack.config.dev.js  # Конфігурація для розробки
├── webpack.config.prod.js # Конфігурація для production
└── package.json           # Залежності та скрипти
```

## 🔨 Збірка для різних середовищ

### Локальна розробка
```bash
npm start
```

### Production (стандартна)
```bash
npm run build
```

### GitHub Pages
```bash
npm run build:github
```
Використовується для автоматичного деплою через GitHub Actions.

### Production з GTM
```bash
npm run build:production
```
Додає Google Tag Manager ID до HTML.

## 🌐 Деплой

### GitHub Pages (автоматичний)

Проєкт автоматично деплоїться на GitHub Pages через GitHub Actions при push в `master` гілку.

Workflow файл: `.github/workflows/webpack.yml`

### Ручний деплой

Зібрану мініфіковану версію з директорії `dist/` можна вручну скопіювати в основний репозиторій або на сервер.

## 📝 Особливості

- **Адаптивний дизайн** - підтримка мобільних та десктопних пристроїв
- **Доступність (a11y)** - використання ARIA атрибутів та семантичної розмітки
- **SEO оптимізація** - мета-теги, Open Graph, Twitter Cards
- **Продуктивність** - lazy loading зображень, оптимізація ресурсів
- **Анімації** - безкінечна прокрутка каруселей, таймер зворотного відліку
- **Мультимовність** - підтримка української та російської мов

## 🎨 Функціональність

- **Таймер зворотного відліку** до 11.11
- **Безкінечна прокрутка** каруселей маркетплейсів та продуктів
- **Мобільне меню** з плавною анімацією
- **Плавна прокрутка** по якорних посиланнях
- **Пауза анімацій** при наведенні миші або фокусі

## 🔍 Налаштування

### Змінні середовища

- `PUBLIC_PATH` - базовий шлях для статичних ресурсів (для GitHub Pages: `/landing_11-11`)
- `GTM_ID` - ID Google Tag Manager (для production: `GTM-MFD96JSQ`)

### Webpack конфігурація

- `webpack.common.js` - загальні налаштування
- `webpack.config.dev.js` - налаштування для розробки (source maps, hot reload)
- `webpack.config.prod.js` - налаштування для production (мініфікація, оптимізація)

## 🐛 Виправлення проблем

### Проблеми з путями до ресурсів

Якщо зображення або стилі не завантажуються:
- Перевірте `PUBLIC_PATH` в webpack конфігурації
- Для GitHub Pages використовуйте `npm run build:github`

### Проблеми з GTM

Якщо Google Tag Manager не працює:
- Перевірте, чи встановлено `GTM_ID` при збірці
- Для production використовуйте `npm run build:production`


## 👥 Розробка

Після внесення змін:

1. Перевірте код лінтером:
```bash
npm run lint:css:fix
```

2. Перевірте локально:
```bash
npm start
```

3. Зберіть production версію:
```bash
npm run build
```

4. Перевірте зібрану версію в `dist/`
